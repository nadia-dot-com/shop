import classes from './Footer.module.css'

export default function Footer() {
    return (
        <footer className={classes.footer}>
            Wszelkie prawa są zastrzeżone &copy;
        </footer>
    )
}